export default function Footer() {
  return (
    <footer className="border-t border-white/5">
      <div className="mx-auto flex max-w-shell flex-col items-start justify-between gap-4 px-6 py-10 text-xs text-ink-400 md:flex-row md:items-center">
        <p>Restructura One — an academic prototype built on synthetic company data.</p>
        <p>Every proposed action is decision support, reviewed by a person before anything happens.</p>
      </div>
    </footer>
  );
}
